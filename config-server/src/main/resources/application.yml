spring:
  application:
    name: config-server
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:git}
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Devanshu2343/config-repo
          default-label: main
          clone-on-start: true

server:
  port: 8888
