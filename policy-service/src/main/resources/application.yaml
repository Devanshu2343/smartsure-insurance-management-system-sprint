server:
  port: 8082

spring:
  application:
    name: policy-service

  datasource:
    url: jdbc:mysql://localhost:3306/policy_db?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/